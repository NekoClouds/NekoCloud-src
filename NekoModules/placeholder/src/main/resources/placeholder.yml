Holograms:
  HOLO_HELLO_WORLD: lobby;0;10;0 # Имя секции == ключ лист из локали

# Если какая то функция не нужна, можно прост стереть с кфг.
DonateLocation: lobby;0;10;0
GameInfoLocation: lobby;0;10;0 # ключ локали HOLO_GAME_INFO_ + режим сам (GRIEF = grief-1, grief-2...)

NPC:
  keker:
    Value: 'value'
    Signature: 'signature'
    Location: 'lobby;1;1;1;1;1'
    Action: 'че выполняем при клике'

    # Чтобы отключить функцию ниже, ее можно просто стереть.
    Name: 'keker1337' # Имя нпс, будет в самом верху голограммы.
    GlowColor: 'AQUA' # Цвет гловинга
    Item: 'STONE' # Что в руку выдать ему, завивит от версии ядра
    HoloKey: 'ключ из локали' # Если его нет, берется имя NPC(либо это имя канала) и добавляется к нему всякая хуйня
    Channel: 'канал режима откуда онлайн брать' # Выбирается из енама ServerMode, убираем если не нужны